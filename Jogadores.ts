export class Jogadores{

    nome: string
    posicao: string
    nivel: number

    constructor(_nome: string, _posicao: string, _nivel: number) {
        
        this.nome = _nome
        this.posicao = _posicao
        this.nivel = _nivel

    }
}